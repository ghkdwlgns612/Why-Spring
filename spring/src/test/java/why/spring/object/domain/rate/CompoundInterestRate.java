package why.spring.object.domain.rate;

import why.spring.object.domain.interestRate;

public class CompoundInterestRate implements interestRate {
    @Override
    public void createInterest(int when, int amount) {
        //로직 생성
    }
}
